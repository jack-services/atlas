# Atlas Environment Variables
# Copy this file to .env and fill in your values
# The .env file is gitignored and will not be committed

# PostgreSQL connection for vector search (with pgvector extension)
# Using pooled connection (recommended for most uses)
ATLAS_DATABASE_URL=postgresql://user:password@host:5432/dbname?sslmode=require

# For uses requiring a connection without pgbouncer (migrations, etc.)
# ATLAS_DATABASE_URL_UNPOOLED=postgresql://user:password@host:5432/dbname?sslmode=require

# OpenAI API key for embeddings (optional - uses Claude by default)
# OPENAI_API_KEY=sk-...
